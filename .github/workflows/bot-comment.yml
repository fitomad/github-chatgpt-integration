name: pull request comment
on:
  issue_comment:                                     
    types: [created, edited ]
jobs:
  pr_commented:
    if: ${{ github.event.issue.pull_request && matches(github.event.comment.body, "globant-bot please review this") }}
    runs-on: [ubuntu-latest]
    steps:
      - run: echo Comentatio de un PR
